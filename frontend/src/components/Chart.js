import React from 'react';

const Chart = ({ data }) => {
    return (
        <div>
            {/* Your chart implementation using a library like Chart.js */}
        </div>
    );
};

export default Chart;
